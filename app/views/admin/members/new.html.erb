<% @page_title = "会員の新規登録" %>

<h1 class="member-list"><%= @page_title %></h1>

<%= form_for [:admin, @member] do |form| %>
  <%= render "shared/member_form", form: form %>
  <div class="btn-admin"><%= form.submit '登録', class: 'btn btn-primary' %></div>
<% end %>
